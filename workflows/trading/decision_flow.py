def decide(symbol: str, granularity: str, cash: float) -> dict:
    # Placeholder: combine agents + metrics into a decision
    return {"action": "HOLD", "quantity": 0, "confidence": 0.5, "reason": "stub"}


